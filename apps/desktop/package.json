{
  "name": "@fnb/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.mjs",
  "scripts": {
    "build:server": "node build.mjs",
    "build": "npm run build:server && npm run build -w @fnb/web",
    "native": "node native.mjs",
    "dev": "npm run build:server && electron .",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "pdfmake": "^0.2.23"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.1",
    "electron": "^33.4.11",
    "esbuild": "^0.28.1"
  }
}
